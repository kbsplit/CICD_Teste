apiVersion: v1
kind: Service
metadata:
  name: video-service 
  labels:
    app: video
spec:
  type: NodePort
  ports:
  - port: 3001
    protocol: TCP
    targetPort: 3001
    nodePort: 30001
  selector:
    app: video